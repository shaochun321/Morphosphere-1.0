# Morphosphere v30 - Hierarchical P Renormalization

v30 promotes stable, evidence-confirmed P structures into **candidate macro-nodes** only after checking sandbox-derived effective-information proxy from v29. Macro nodes are not source facts and are not active controllers.

Run:

```bash
python3 active/v30/scripts/check_v30.py --db outputs/m30.db
python3 active/v30/scripts/query_v30.py --db outputs/m30.db --limit 5
```
