# Morphosphere v35 Engineered Bridge Overlay

This is an overlay package, not a full baseline package. Apply it to an existing v34 or later Morphosphere tree:

```bash
./APPLY_TO_EXISTING_TREE.sh /path/to/Morphosphere_tree
```

It adds sandbox-only v35 attention governance and path-integral audit tables without rewriting source facts.
