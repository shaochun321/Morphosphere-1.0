# Morphosphere v36.6 Pass8

Pass8 is a boundary cleanup and full-chain-run alignment pass.

It does not remove engineering improvements. It classifies them:

- Blueprint core: process window, coordinate-hidden measure binding, external envelope ref, semantic-null guard, hyperedge/process relation, Xin carrier binding, external readout-only contract, downgrade contract.
- Full-chain materialized data: information point backprojection, trajectory/TOPRXin, counter-evidence/masking, ledger, attention, hyperedge, variational/coupler, carrier/readout.
- External modules: read-only external Xin definition and semantic readout modules.
- Test/operability: query scripts, health scores, collaboration edges, deployment checks.
- Advisory: future native write contracts and directness debt indexes.

Run:

```bash
./RUN_DEPLOY_CHECKS.sh
./RUN_V366_PASS8_CHECKS.sh
./RUN_PASS8_BOUNDARY_SUMMARY.sh
```
