# Morphosphere CTC Extraction v2.1 Full Package

This archive is a flat, complete local deployment package.
Unzip it, enter `morphosphere_ctc_extraction_v21_full_package/`, then run `./run_local_ctc_extraction_v21.sh`.

It is not an overlay package and does not require a separate base archive.
