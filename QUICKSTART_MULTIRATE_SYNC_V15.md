# Quickstart v1.5

```bash
cd morphosphere_multirate_sync_v15_package
./run_local_multirate_sync_v15.sh
```

The script verifies that v1.5 tables and runtime sidecars are present, then runs the v1.5 acceptance checks.
