# Pass6 deduplication note

Large duplicate database aliases are preserved as symlinks to keep legacy paths while reducing the tar.zst size. The canonical files remain in `outputs/` and `outputs/v366/`.
